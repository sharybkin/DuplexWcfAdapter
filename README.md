# DuplexWcfAdapter
Example of replacing a Duplex WCF service with backward compatibility.

docker compose is used only to run .Net Core service and RabbitMq.
WcfTransmitter.Client and WcfTransmitter.Service must be runned manually.
